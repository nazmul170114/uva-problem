#include<cstdio>
#include<cmath>
using namespace std;
int main()
{
    long long n,a,b;
    while(scanf("%lld",&n)!=EOF){
        if(n==0)
           break;
        a=sqrt(n);
        b=a*a;
        if(b==n)
           printf("yes\n");
        else
           printf("no\n");
    }
    return(0);
}
